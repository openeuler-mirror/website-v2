# server

#### install dependencies:

  > npm install

#### run the app:

  > SET DEBUG=server:* & npm start

#### dependencies

  > npm install file-stream-rotator
  > npm install helmet
  > npm install request
  > npm install mysql
  > npm install cheerio
  
